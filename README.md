
  # Create wireframes

  This is a code bundle for Create wireframes. The original project is available at https://www.figma.com/design/MRMgM7Ei4dmMYrSaYOkeI8/Create-wireframes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  